services:
  app:
    build:
      context: .
      target: development
    environment:
      NODE_ENV: development
      PORT: 8080
    init: true
    ports:
      - "8080:8080"
      - "9229:9229"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
